# Web UI usage

1. Open the deployed Worker URL (or `http://localhost:8787` locally).
2. Sign in with GitHub.
3. Create a project (slug + display name).
4. Create an API key scoped to a project with `read` or `write` permissions.
5. Add, reveal, and delete secrets from the UI.

API keys are shown only once when created; copy immediately.
